# 2026-07-21 工作日志

## 《求是》杂志 2026 年文章采集（知识库材料）

- 数据源：求是网 qstheory.cn（公开，无需登录）。链路：mulu.htm → 年索引 → 各期目录 → 文章全文。
- 正文结构：容器 `.text`、标题 `<h1>`、作者 `.appellation`、日期 `.pubtime`、来源 `《求是》2026/NN`，正文为真实 HTML 文本。
- 范围（用户确认）：2026 年第 1–14 期、全部文章。交付：单篇可编辑 PDF + 每期合订 PDF + Markdown/JSON 索引。
- 关键坑：mulu.htm 与年索引里的链接是相对路径（如 `../20251231/<hash>/c.html`），原 `norm()` 不解析相对路径 → 改用 `urllib.parse.urljoin(base, ...)`，三处调用补 base。
- 第二个坑：目录页含「往期/其他期次」导航链接（形如《求是》2025年第X期），其 href 也匹配文章正则，被误抓为 14 条空标题 junk。已在 get_articles 增加过滤（跳过空标题 + 跳过含「年第\d+期」的导航链接），并删除 14 个 untitled 文件后重跑。
- 产出：292 篇真实单篇 PDF + 14 期合订 PDF + index.json/csv + README.md，位于 `qiushi_2026/`。
- 可编辑性验证：用 pypdf 抽取文字成功，确认文字层可选中复制，适合知识库。
- 脚本：`crawl_qiushi.py`（托管 Python venv：`C:/Users/14250/.workbuddy/binaries/python/envs/default/`）。
- 注：二十届四中全会 2025-10-20~23 召开；用户原话「四中全会以后」但确认只取 2026 年 1–14 期（若要含 2025 年 21–24 期需再扩）。

## 技能沉淀
- 从本案例抽象出**通用技能** `web-article-collector`（用户级 `~/.workbuddy/skills/`）：四阶段=对齐范围→侦察站点结构→稳健抓取(相对路径urljoin+导航误抓过滤+礼貌限速+可续跑)→生成可编辑PDF(reportlab+STSong-Light CID字体)+索引(json/csv)+pypdf校验。去掉《求是》写死参数，保留通用模板与坑位；`qiushi-magazine-crawler` 作为其具体实例保留。
- 触发场景：党刊/学报/新闻专栏/企业内刊/法规公报/博客专栏等「网站=一堆带独立详情页的文章」且要可编辑PDF或结构化索引入库。

## web-article-collector 增强：时间戳归一化 + 信源注册表 + 日期筛选（日报前置）
- 新增 `lib/timestamp.py`：多策略提取(JSON-LD→OG/meta→time[datetime]→信源time_selector→带标签页眉→相对时间)，置信度 high/medium/low/none。**防误识别**第一：不扫正文、剔除评论/相关推荐/页脚/导航、页眉时间需邻近「发布/日期/来源」标签词、合理性校验(≥1990且不晚于抓取时刻)。默认严格模式：仅 high/medium，相对/无标签时间返回 None，不瞎猜。
- 时区归一化：优先用时间自带偏移；无时区按信源 default_tz 推断，输出 published_utc + published_beijing。转换北京=纯工程。
- 新增 `config/sources.example.json`（含求是网实例，时区跟信源走）、`lib/filter_daily.py`（by_beijing_date/by_range 筛选仅 high/medium；aggregate_daily 分组+去重+排序+to_markdown；low 单独列待核对）。
- 自测通过：正文 2020/2019、相关推荐、评论干扰时间均未被误抓；相对时间严格=None、非严格=low。
- 坑：`<time>` 若是「3小时前」相对时间不能当 high，需降级 time:relative。
- 设计哲学（用户明确要求）：**宁可识别不出，也不能识别错**。
